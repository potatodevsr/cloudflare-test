{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create tasks-db",
    "db:migrate": "wrangler d1 migrations apply tasks-db --remote",
    "db:migrate:local": "wrangler d1 migrations apply tasks-db --local",
    "db:generate": "prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-d1": "^5.22.0",
    "@prisma/client": "^5.22.0",
    "hono": "^4.6.14",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "prisma": "^5.22.0",
    "typescript": "^5.9.3",
    "wrangler": "^3.96.0"
  }
}
